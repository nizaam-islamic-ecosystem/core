# Nizaam Core

`nizaam-core` is the Rust library of shared, domain agnostic contracts and mechanisms for Nizaam Domain Engines and Infrastructure Engines.

It is not an executable and contains no engine specific domain semantics. Architecture and implementation progress are tracked in [scope.md](scope.md).
